# Same publishable key as the root .env (safe to expose to the browser)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Production builds only: the deployed backend origin. Leave unset in dev —
# requests then go through Vite's /api proxy to localhost:3001.
# VITE_API_URL=https://fluentkeys-backend.onrender.com
